name: Manual
on: 
    workflow_dispatch:
        inputs:
            log-level:
                    type: choice
                    options:
                        - info
                        - warn
                        - error
jobs:
    print:
        runs-on: ubuntu-latest
        env:
            username: ${{ vars.USERNAME }}
        steps:
            - run: echo "${{username}}"